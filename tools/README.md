# Generating ASM Patches

You will first need to install `keystone-engine` by running `uv pip install keystone-engine`. This is required if you are updating the current ASM patches or wish to create new ones.

After `keystone-engine` is installed, run `uv run tools\create_asm_patches.py`. This will generate the necessary binary files.
